# 🚀 Настройка автоматического деплоя через GitHub Actions

Руководство по настройке автоматического развертывания на production сервер при каждом push в main.

## ✅ Что уже настроено

- ✅ GitHub Actions workflow создан (`.github/workflows/deploy-production.yml`)
- ✅ Скрипт деплоя настроен
- ⏳ Нужно добавить секреты в GitHub

---

## 🔧 Настройка

### Шаг 1: Создание SSH ключа на сервере

Подключитесь к серверу и создайте SSH ключ специально для GitHub Actions:

```bash
ssh root@91.229.11.132

# Создайте SSH ключ
ssh-keygen -t ed25519 -f ~/.ssh/github_actions -N ""

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/github_actions
chmod 644 ~/.ssh/github_actions.pub

# Скопируйте приватный ключ (понадобится для GitHub)
cat ~/.ssh/github_actions
```

Скопируйте весь вывод последней команды (от `-----BEGIN` до `-----END`).

### Шаг 2: Добавление секретов в GitHub

1. Откройте ваш репозиторий на GitHub: https://github.com/Psayha/salmina

2. Перейдите в **Settings** → **Secrets and variables** → **Actions**

3. Нажмите **New repository secret** и добавьте три секрета:

#### VPS_HOST
```
91.229.11.132
```

#### VPS_USERNAME
```
root
```

#### VPS_SSH_KEY
```
(вставьте приватный ключ из Шага 1)
```

Должно выглядеть примерно так:
```
-----BEGIN OPENSSH PRIVATE KEY-----
b3BlbnNzaC1rZXktdjEAAAAABG5vbmUAAAAEbm9uZQAAAAAAAAABAAAAMwAAAAtzc2gtZW
...
(много строк)
...
-----END OPENSSH PRIVATE KEY-----
```

### Шаг 3: Проверка настройки

После добавления всех секретов:

1. Сделайте любое изменение в коде:
   ```bash
   echo "# Test auto-deploy" >> README.md
   git add README.md
   git commit -m "test: проверка автодеплоя"
   git push origin main
   ```

2. Откройте страницу Actions в GitHub:
   ```
   https://github.com/Psayha/salmina/actions
   ```

3. Вы должны увидеть запущенный workflow "Deploy to Production"

4. Дождитесь завершения (обычно 2-3 минуты)

5. Проверьте, что изменения появились на сервере:
   ```bash
   ssh root@91.229.11.132
   cd /var/www/telegram-shop
   git log -1
   ```

---

## 📋 Как работает автодеплой

При каждом `git push origin main`:

1. **GitHub Actions** получает код
2. **Подключается к серверу** по SSH
3. **Выполняет команды**:
   ```bash
   cd /var/www/telegram-shop
   git pull origin main
   pnpm install --frozen-lockfile
   cd apps/backend && pnpm build
   cd apps/frontend && pnpm build
   pm2 restart telegram-shop-backend
   pm2 restart telegram-shop-frontend
   pm2 save
   pm2 status
   ```
4. **Проверяет работу** сервисов:
   - Backend: `https://app.salminashop.ru/health`
   - Frontend: `https://salminashop.ru`

---

## 🎯 Текущая ситуация

**Проблема:** GitHub Actions упал с ошибкой `missing server host`

**Причина:** Секреты `VPS_HOST`, `VPS_USERNAME`, `VPS_SSH_KEY` не добавлены в GitHub

**Решение:** Выполните Шаги 1-3 выше

---

## 🧪 Тестирование

### Успешный деплой

При успешном деплое вы увидите:

```
✓ Deployment successful!
✓ Backend health check passed
✓ Frontend health check passed
```

### Проверка на сервере

```bash
# Посмотреть статус сервисов
pm2 status

# Посмотреть логи backend
pm2 logs telegram-shop-backend --lines 50

# Посмотреть логи frontend
pm2 logs telegram-shop-frontend --lines 50

# Проверить последний коммит
cd /var/www/telegram-shop
git log -1
```

---

## 🐛 Решение проблем

### Error: missing server host

**Проблема:** Секреты не добавлены в GitHub

**Решение:** Добавьте `VPS_HOST`, `VPS_USERNAME`, `VPS_SSH_KEY` в Settings → Secrets

### Error: Permission denied (publickey)

**Проблема:** SSH ключ не добавлен на сервер или неправильный

**Решение:**
1. Проверьте, что публичный ключ добавлен в `~/.ssh/authorized_keys`
2. Проверьте права: `chmod 600 ~/.ssh/authorized_keys`
3. Пересоздайте ключ (Шаг 1)

### Build failed

**Проблема:** Ошибка компиляции TypeScript

**Решение:**
1. Проверьте локально: `pnpm tsc --noEmit`
2. Исправьте ошибки
3. Закоммитьте и запушьте снова

### PM2 restart failed

**Проблема:** Сервисы не перезапускаются

**Решение:**
1. Подключитесь к серверу
2. Проверьте статус: `pm2 status`
3. Посмотрите логи: `pm2 logs`
4. Перезапустите вручную: `pm2 restart all`

---

## 📚 Дополнительные возможности

### Ручной запуск деплоя

Workflow можно запустить вручную через GitHub Actions UI:

1. Откройте https://github.com/Psayha/salmina/actions
2. Выберите "Deploy to Production"
3. Нажмите "Run workflow"
4. Выберите ветку `main`
5. Нажмите "Run workflow"

### Деплой на другие окружения

Можно создать дополнительные workflow для staging/dev:

```yaml
# .github/workflows/deploy-staging.yml
on:
  push:
    branches:
      - develop
```

---

## ✅ Чеклист готовности

После настройки проверьте:

- [ ] SSH ключ создан на сервере
- [ ] Публичный ключ добавлен в authorized_keys
- [ ] Секреты добавлены в GitHub (VPS_HOST, VPS_USERNAME, VPS_SSH_KEY)
- [ ] Тестовый push выполнен успешно
- [ ] GitHub Actions workflow завершился без ошибок
- [ ] Изменения появились на production сервере
- [ ] Backend доступен: https://app.salminashop.ru/health
- [ ] Frontend доступен: https://salminashop.ru

---

## 🎉 Готово!

После настройки каждый `git push origin main` будет автоматически деплоить код на production сервер!

**Процесс разработки:**
```bash
# 1. Внесите изменения
git add .
git commit -m "feat: добавил новую фичу"

# 2. Запушьте в main
git push origin main

# 3. GitHub Actions автоматически задеплоит на сервер
# 4. Через 2-3 минуты изменения будут на production
```

---

**Статус:** ⏳ Ожидает настройки секретов в GitHub

**Следующий шаг:** Добавьте секреты по инструкции выше
