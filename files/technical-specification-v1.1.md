# 📋 ПОЛНОЕ ТЕХНИЧЕСКОЕ ЗАДАНИЕ (v1.1)
## Telegram Mini App - Интернет-магазин косметики

**Дата обновления:** 2024-11-11  
**Версия:** 1.1 (Updated)

---

## 🎯 ОБЩАЯ ИНФОРМАЦИЯ

**Тип проекта:** Telegram Mini App (Web App)  
**Предметная область:** Интернет-магазин косметики  
**Язык интерфейса:** Русский  
**Валюта:** Рубли (₽)  
**Платформа:** Telegram (iOS, Android, Desktop, Web)

---

## 🏗️ ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Frontend
- **Framework:** Next.js 14+ (App Router)
- **Language:** TypeScript (strict mode)
- **Styling:** Tailwind CSS
- **State Management:** Zustand
- **Forms:** React Hook Form + Zod validation
- **HTTP Client:** Axios с interceptors
- **Telegram SDK:** @telegram-apps/sdk
- **Animations:** Framer Motion
- **QR Code:** qrcode.react (для wishlist sharing)

### Backend
- **Runtime:** Node.js (v20+)
- **Framework:** Express.js / Fastify
- **Language:** TypeScript (strict mode)
- **ORM:** Prisma
- **Database:** PostgreSQL 15+
- **Authentication:** JWT (access + refresh tokens)
- **Session Storage:** Redis (для токенов сессии 30 дней)

### DevOps
- **Containerization:** Docker + Docker Compose
- **CI/CD:** GitHub Actions
- **Hosting:** VPS
- **Reverse Proxy:** Nginx
- **SSL:** Let's Encrypt

### Integrations
- **Payment:** Prodamus API (СБП)
- **Telegram:** Bot API для уведомлений
- **Telegram:** Mini Apps API
- **QR Code Generation:** для wishlist sharing

---

## 📁 СТРУКТУРА ПРОЕКТА

```
telegram-shop/
├── frontend/                      # Next.js приложение
│   ├── app/
│   │   ├── (user)/               # Пользовательские роуты
│   │   │   ├── page.tsx          # Главная (с акциями + метками категорий)
│   │   │   ├── catalog/          # Каталог
│   │   │   ├── product/          # Товар
│   │   │   ├── cart/             # Корзина
│   │   │   ├── checkout/         # Оформление
│   │   │   ├── profile/          # Профиль
│   │   │   ├── favorites/        # Избранное (с sharing)
│   │   │   ├── orders/           # Заказы
│   │   │   ├── delivery-payment/ # Доставка и оплата (одна страница)
│   │   │   ├── legal/            # Юр. документы
│   │   │   └── shared-wishlist/[id] # Публичный wishlist
│   │   └── (admin)/              # Админ панель
│   │       ├── login/
│   │       └── dashboard/        # С табами (контент/статистика/пользователи)
│   ├── components/
│   │   ├── ui/                   # UI kit
│   │   ├── features/
│   │   ├── layouts/
│   │   └── modals/               # Модальное меню и др.
│   ├── lib/
│   ├── hooks/
│   ├── store/
│   └── types/
│
├── backend/
│   ├── src/
│   │   ├── modules/
│   │   │   ├── auth/
│   │   │   ├── products/
│   │   │   ├── orders/
│   │   │   ├── users/
│   │   │   ├── cart/
│   │   │   ├── favorites/
│   │   │   ├── categories/
│   │   │   ├── promocodes/
│   │   │   ├── promotions/       # Акции для главной
│   │   │   ├── notifications/
│   │   │   ├── analytics/
│   │   │   └── legal/            # Юр. документы
│   │   ├── common/
│   │   ├── config/
│   │   └── utils/
│   ├── prisma/
│   └── tests/
│
├── bot/                           # Telegram Bot
└── docs/                          # Документация
```

---

## 🗄️ СТРУКТУРА БАЗЫ ДАННЫХ

### users
```typescript
{
  id: string (UUID)
  telegramId: bigint (unique)
  username: string?
  firstName: string
  lastName: string?
  photoUrl: string?
  phone: string?
  email: string?
  role: enum ('USER', 'ADMIN')
  isActive: boolean
  hasAcceptedTerms: boolean // Принял ли юр. документы
  termsAcceptedAt: timestamp?
  createdAt: timestamp
  updatedAt: timestamp
}
```

### categories
```typescript
{
  id: string (UUID)
  name: string
  slug: string (unique)
  description: string?
  parentId: string? (self-reference)
  order: integer
  image: string? // Для меток на главной (1:1)
  showOnHome: boolean // Показывать на главной
  homeOrder: integer? // Порядок на главной
  isActive: boolean
  createdAt: timestamp
  updatedAt: timestamp
}
```

### products
```typescript
{
  id: string (UUID)
  categoryId: string (FK)
  name: string
  slug: string (unique)
  description: text
  price: decimal(10,2) // Основная цена
  promotionPrice: decimal(10,2)? // Акционная цена (если есть)
  discountPrice: decimal(10,2)? // Цена со скидкой (менее приоритетна)
  article: string (unique)
  sku: string (unique)
  weight: decimal(8,2)
  dimensions: string?
  quantity: integer
  composition: text?
  delivery: text?
  characteristics: json?
  application: text?
  images: string[] // До 4 изображений
  isNew: boolean
  isHit: boolean
  isDiscount: boolean
  hasPromotion: boolean // Если true - промокоды НЕ работают
  viewCount: integer
  orderCount: integer
  isActive: boolean
  createdAt: timestamp
  updatedAt: timestamp
}
```

### promotions (НОВАЯ)
```typescript
{
  id: string (UUID)
  title: string
  description: text?
  image: string // 1:1 соотношение (квадратное)
  link: string? // URL категории/товара
  order: integer
  isActive: boolean
  validFrom: timestamp?
  validTo: timestamp?
  createdAt: timestamp
  updatedAt: timestamp
}
```

### wishlist_shares (НОВАЯ)
```typescript
{
  id: string (UUID)
  userId: string (FK)
  shareId: string (unique) // Для URL типа /shared-wishlist/abc123
  title: string? // Название wishlist
  qrCodeUrl: string? // URL сгенерированного QR кода
  isActive: boolean
  viewCount: integer
  createdAt: timestamp
  expiresAt: timestamp? // Опционально
}
```

### cart_items
```typescript
{
  id: string (UUID)
  cartId: string (FK)
  productId: string (FK)
  quantity: integer
  price: decimal(10,2) // Базовая цена
  appliedPrice: decimal(10,2) // Итоговая (с учетом акций/скидок)
  hasPromotion: boolean // Snapshot - была ли акция
  allowPromocode: boolean // Можно ли применить промокод
  createdAt: timestamp
}
```

### promocodes
```typescript
{
  id: string (UUID)
  code: string (unique) // Задается админом вручную
  discountType: enum ('PERCENT', 'FIXED')
  discountValue: decimal(10,2)
  minOrderAmount: decimal(10,2)?
  maxUses: integer?
  usedCount: integer
  isActive: boolean
  validFrom: timestamp
  validTo: timestamp
  createdAt: timestamp
  updatedAt: timestamp
}
```

### legal_documents (НОВАЯ)
```typescript
{
  id: string (UUID)
  type: enum ('TERMS', 'PRIVACY', 'OFFER', 'DELIVERY_PAYMENT') // DELIVERY_PAYMENT - новый тип
  title: string
  content: text // Rich text / HTML
  version: string // Версия документа
  isActive: boolean
  publishedAt: timestamp?
  createdAt: timestamp
  updatedAt: timestamp
}
```

### orders
```typescript
{
  id: string (UUID)
  orderNumber: string (unique)
  userId: string (FK)
  status: enum ('PAID', 'PROCESSING', 'SHIPPED', 'CANCELLED')
  
  // Данные покупателя
  customerName: string
  customerPhone: string
  customerEmail: string
  customerAddress: text
  comment: text?
  
  // Финансы
  subtotal: decimal(10,2) // Сумма товаров
  discount: decimal(10,2) // Скидка от промокода
  total: decimal(10,2) // Итого
  
  // Промокод (если применен и разрешен)
  promocodeId: string? (FK)
  promocodeDiscount: decimal(10,2)?
  
  // Доставка
  trackingNumber: string?
  
  // Оплата
  paymentMethod: enum ('SBP', 'CASH_ON_DELIVERY')
  paymentStatus: enum ('PENDING', 'PAID', 'FAILED')
  paymentId: string?
  
  createdAt: timestamp
  updatedAt: timestamp
  paidAt: timestamp?
  shippedAt: timestamp?
}
```

### order_items
```typescript
{
  id: string (UUID)
  orderId: string (FK)
  productId: string (FK)
  
  // Snapshot данных товара
  productName: string
  productArticle: string
  productImage: string
  basePrice: decimal(10,2) // Базовая цена
  appliedPrice: decimal(10,2) // Фактическая (с акцией/скидкой)
  hadPromotion: boolean // Была ли акция
  quantity: integer
  
  createdAt: timestamp
}
```

---

## 🎨 ФУНКЦИОНАЛ ПО РАЗДЕЛАМ

### 🚀 ЗАГРУЗОЧНЫЙ ЭКРАН (НОВОЕ)

**Что происходит при запуске приложения:**

1. **Splash Screen**
   - Логотип магазина
   - Индикатор загрузки
   - Текст: "Загрузка..."

2. **Получение данных Telegram**
   - initData от Telegram
   - Парсинг user данных (id, username, photo, etc.)
   - Валидация данных

3. **Проверка принятия юр. документов**
   - Если `user.hasAcceptedTerms = false`:
     - Показать модальное окно с юр. документами
     - Чекбоксы для каждого документа:
       - [ ] Я принимаю условия использования
       - [ ] Я согласен с политикой конфиденциальности
       - [ ] Я ознакомлен с публичной офертой
     - Кнопка "Продолжить" (активна только если все чекбоксы)
     - После принятия: `hasAcceptedTerms = true`, `termsAcceptedAt = NOW()`
   
4. **Инициализация приложения**
   - Загрузка настроек
   - Восстановление корзины (если есть session token)
   - Переход на главную страницу

5. **Telegram Fullscreen**
   - Использовать `viewport.expand()` для fullscreen режима
   - Применить автоматически при запуске

**Компоненты:**
```typescript
// components/SplashScreen.tsx
// components/LegalAcceptanceModal.tsx
// hooks/useAppInitialization.ts
```

---

### 1. ГЛАВНАЯ СТРАНИЦА (/) - ОБНОВЛЕНО

**Структура (сверху вниз):**

1. **Header**
   - Логотип
   - Кнопка меню (гамбургер) → открывает модальное окно
   - Кнопка корзины с badge
   - Поиск (иконка)

2. **Акции (НОВОЕ)**
   - Заголовок секции: "Акции" (h2)
   - Горизонтальный скролл плиток
   - Карточка акции:
     - Квадратное изображение (1:1, например 300x300px)
     - Заголовок акции (оверлей на изображении)
     - При клике → переход по ссылке (категория/товар)
   - Плитки с небольшим gap между ними
   - Smooth scroll

3. **Метки категорий (НОВОЕ)**
   - Заголовок: "Категории" или скрыт (опционально)
   - Горизонтальный скролл меток
   - Карточка метки:
     - Квадратное изображение (1:1, размер ~80-100px)
     - Название категории снизу
     - Border radius для скругления
     - При клике → переход в эту категорию
   - Админ выбирает какие категории показывать (`showOnHome = true`)
   - Сортировка по `homeOrder`

4. **Товары**
   - Популярные товары (топ по orderCount)
   - Новинки (isNew = true)
   - Хиты продаж (isHit = true)
   - Товары со скидкой (isDiscount = true)
   - Grid layout (2 колонки на мобилке, 3-4 на десктопе)

**Модальное меню (НОВОЕ):**

Открывается при клике на гамбургер:

```
┌─────────────────────────┐
│  [X]  Меню              │
├─────────────────────────┤
│  🏠 Главная             │
│  📦 Каталог             │
│  ❤️  Избранное          │
│  📋 Заказы              │
│  👤 Профиль             │
│  🚚 Доставка и оплата   │ ← Новое
│  📄 Юр. документы       │ ← Новое
│  ℹ️  О нас              │
└─────────────────────────┘
```

- Backdrop затемнение
- Анимация появления (slide from left/right)
- Закрытие по клику на backdrop или крестик
- Haptic feedback при выборе пункта

---

### 📄 СТРАНИЦА "ДОСТАВКА И ОПЛАТА" (НОВОЕ)

**URL:** `/delivery-payment`

**Содержимое:**

Одна страница с двумя секциями:

1. **Секция "Доставка"**
   - Заголовок "Доставка"
   - Rich text контент из БД (`legal_documents` где `type = 'DELIVERY_PAYMENT'`)
   - Информация о СДЭК
   - Сроки доставки
   - Стоимость
   - Условия

2. **Секция "Оплата"**
   - Заголовок "Оплата"
   - Способы оплаты:
     - СБП (описание)
     - Оплата при получении (описание про наложенный платеж)
   - Безопасность платежей
   - Возврат средств

**Редактирование в админке:**

Админ может редактировать весь контент этой страницы через rich text editor.

---

### 2. КАТАЛОГ (/catalog)

**Без изменений**, всё остается как было:
- Двухуровневая система категорий
- Поиск и фильтрация
- Сортировка (популярные, дешевые, дорогие)

---

### 3. КАРТОЧКА ТОВАРА (/product/[slug]) - ОБНОВЛЕНО

**Изменения в ценообразовании:**

```typescript
// Логика отображения цены
if (product.hasPromotion && product.promotionPrice) {
  // Акционная цена (наивысший приоритет)
  displayPrice = product.promotionPrice
  originalPrice = product.price
  showPromoBadge = true
  allowPromocode = false // ❗ ПРОМОКОД НЕ РАБОТАЕТ
} else if (product.isDiscount && product.discountPrice) {
  // Скидка
  displayPrice = product.discountPrice
  originalPrice = product.price
  showDiscountBadge = true
  allowPromocode = true // Промокод работает
} else {
  // Обычная цена
  displayPrice = product.price
  allowPromocode = true
}
```

**Отображение:**
```
[Зачеркнутая цена] 1 999 ₽
[Акционная цена]   999 ₽
[Badge] АКЦИЯ
```

**При добавлении в корзину:**
- Сохраняется `appliedPrice` (текущая цена)
- Сохраняется `hasPromotion`
- Сохраняется `allowPromocode`

---

### 4. КОРЗИНА (/cart) - ОБНОВЛЕНО

**Логика расчета с учетом акций и промокодов:**

```typescript
// Для каждого товара в корзине
cartItems.forEach(item => {
  subtotal += item.appliedPrice * item.quantity
  
  if (item.allowPromocode) {
    itemsAllowingPromocode.push(item)
  }
})

// Применение промокода
if (promocode && itemsAllowingPromocode.length > 0) {
  const discountableAmount = itemsAllowingPromocode.reduce(
    (sum, item) => sum + item.appliedPrice * item.quantity, 
    0
  )
  
  if (promocode.discountType === 'PERCENT') {
    discount = discountableAmount * (promocode.discountValue / 100)
  } else {
    discount = promocode.discountValue
  }
}

total = subtotal - discount
```

**Отображение в корзине:**
```
Товар 1 (акция)        999 ₽ x 2 = 1 998 ₽  🔒 Промокод не применяется
Товар 2                500 ₽ x 1 =   500 ₽  ✅ Промокод применяется
─────────────────────────────────────────
Сумма:                                2 498 ₽
Промокод SALE10:                       -50 ₽  (применен только к Товару 2)
─────────────────────────────────────────
Итого:                                2 448 ₽
```

**Валидация промокода:**
- Проверка что есть хотя бы один товар с `allowPromocode = true`
- Если все товары с акцией → показать сообщение:
  ```
  ℹ️ Промокод не может быть применен: все товары в корзине 
     уже участвуют в акции
  ```

---

### 5. ИЗБРАННОЕ (WISHLIST) - ОБНОВЛЕНО

**Новый функционал - Публичный Wishlist:**

#### В личном избранном (/favorites):

**Добавлена кнопка "Поделиться":**
```
┌────────────────────────────────┐
│  ❤️  Избранное (5)             │
├────────────────────────────────┤
│  [Поделиться списком] 🔗       │
└────────────────────────────────┘
```

**При клике на "Поделиться":**

1. **Модальное окно:**
   ```
   ┌─────────────────────────────────┐
   │  Поделиться избранным           │
   ├─────────────────────────────────┤
   │  Название списка (опционально): │
   │  [________________]             │
   │                                 │
   │  [QR Код]                       │
   │                                 │
   │  Ссылка:                        │
   │  https://t.me/bot/app?          │
   │  startapp=wishlist_abc123       │
   │  [Копировать]                   │
   │                                 │
   │  [Поделиться в Telegram]        │
   │  [Скачать QR код]               │
   └─────────────────────────────────┘
   ```

2. **Генерация share:**
   - Создается запись в `wishlist_shares`
   - Генерируется уникальный `shareId`
   - Создается QR код с ссылкой
   - Сохраняется в БД

3. **Действия:**
   - **Копировать** - копирует ссылку в буфер + toast
   - **Поделиться в Telegram** - `shareURL()` от Telegram SDK
   - **Скачать QR код** - скачивание PNG

#### Публичная страница wishlist (/shared-wishlist/[shareId]):

**Что видят другие пользователи:**
```
┌─────────────────────────────────┐
│  ❤️  Wishlist: "Мой список"     │
│     от @username                │
├─────────────────────────────────┤
│  [Карточка товара 1]            │
│  [Карточка товара 2]            │
│  [Карточка товара 3]            │
└─────────────────────────────────┘
```

**Функционал:**
- Просмотр всех товаров из wishlist
- Клик на товар → переход на страницу товара
- Кнопка "Добавить все в корзину"
- Счетчик просмотров (увеличивается)
- Если wishlist пустой или неактивен → 404 или "Список не найден"

**API Endpoints:**
```
POST   /api/favorites/share          # Создать публичную ссылку
GET    /api/favorites/share/:id      # Получить данные share
DELETE /api/favorites/share/:id      # Удалить публичную ссылку
GET    /api/shared-wishlist/:shareId # Получить товары публичного wishlist
```

---

### 🔐 АДМИН ПАНЕЛЬ (/admin/dashboard) - ОБНОВЛЕНО

**Новая структура - Табы вместо отдельных страниц:**

```
┌──────────────────────────────────────────────────┐
│  Админ панель                        [@admin]    │
├──────────────────────────────────────────────────┤
│  [Контент] [Статистика] [Пользователи]           │ ← Табы
├──────────────────────────────────────────────────┤
│                                                  │
│  [Содержимое активного таба]                     │
│                                                  │
└──────────────────────────────────────────────────┘
```

### Таб 1: КОНТЕНТ

**Подразделы (вложенные табы или аккордеон):**

1. **Товары**
   - Список товаров (таблица)
   - CRUD операции
   - Фильтры, поиск
   - Форма товара (убрана вкладка SEO) ❗

2. **Категории**
   - Древовидный список
   - CRUD операции
   - Настройка отображения на главной:
     - [ ] Показывать на главной (`showOnHome`)
     - Порядок на главной (`homeOrder`)
     - Загрузка изображения для метки (1:1)

3. **Акции** ❗ НОВОЕ
   - Список акций (таблица)
   - Создание/редактирование:
     - Заголовок *
     - Описание
     - Изображение * (1:1, рекомендуется 600x600px)
     - Ссылка (URL категории или товара)
     - Порядок
     - Период показа (от/до)
     - Статус (Активна/Неактивна)
   - Drag & drop для изменения порядка

4. **Заказы**
   - Список заказов
   - Детали заказа
   - Изменение статуса
   - Отмена

5. **Промокоды** ❗ ОБНОВЛЕНО
   - Список промокодов
   - Создание промокода:
     - **Код вводится вручную админом** (НЕ генерируется случайно)
     - Валидация: только заглавные буквы и цифры
     - Проверка уникальности
     - Остальные поля без изменений
   - Редактирование
   - Удаление

6. **Страницы**
   - Список статичных страниц:
     - Доставка и оплата ❗ (одна страница)
     - О нас
     - Контакты
   - Rich text editor для редактирования

7. **Юридические документы** ❗ НОВОЕ
   - Список документов:
     - Условия использования
     - Политика конфиденциальности
     - Публичная оферта
   - Редактирование через Rich Text Editor
   - Версионирование:
     - При сохранении создается новая версия
     - Старые версии хранятся (для истории)
     - Можно просмотреть изменения
   - Статус (Активна/Черновик)

### Таб 2: СТАТИСТИКА

**Метрики (карточки):**
- 💰 Оплачено (сумма PAID за период)
- 📦 Отгружено (количество SHIPPED)
- ⏳ В обработке (PAID + PROCESSING)
- 🛍️ Всего заказов

**Графики:**
- График заказов по дням (30 дней)
- График выручки по дням (30 дней)
- Топ товаров по продажам
- Топ категорий

**Фильтры:**
- Выбор периода (неделя/месяц/квартал/год/custom)

### Таб 3: ПОЛЬЗОВАТЕЛИ

**Список пользователей:**
- Таблица:
  - Фото
  - Имя
  - Username
  - Telegram ID
  - Роль
  - Принял юр. документы ❗
  - Дата регистрации
  - Действия

**Просмотр пользователя:**
- Вся информация
- История заказов
- Дата принятия юр. документов ❗
- Кнопка "Написать в Telegram"

**Изменение роли:**
- Только ADMIN может назначать админов

---

## 📱 TELEGRAM MINI APP ИНТЕГРАЦИЯ - ОБНОВЛЕНО

### Fullscreen Mode ❗ НОВОЕ

```typescript
import { viewport } from '@telegram-apps/sdk';

// При запуске приложения
useEffect(() => {
  viewport.expand(); // Развернуть на весь экран
  
  // Можно также использовать
  viewport.requestFullscreen(); // Принудительный fullscreen (если поддерживается)
}, []);
```

**Когда использовать:**
- Автоматически при запуске приложения
- После принятия юр. документов
- При переходе на страницы с контентом

### Остальные интеграции

Без изменений (MainButton, BackButton, Haptic, Theme, Sharing, Popup).

---

## 🔗 API ENDPOINTS - ОБНОВЛЕНО

### Новые endpoints:

#### Promotions
```
GET    /api/promotions              # Активные акции для главной
GET    /api/admin/promotions        # Список (админ)
POST   /api/admin/promotions        # Создать
PUT    /api/admin/promotions/:id    # Обновить
DELETE /api/admin/promotions/:id    # Удалить
POST   /api/admin/promotions/reorder # Изменить порядок
```

#### Categories (обновлено)
```
GET /api/categories/home            # Категории для главной (showOnHome=true)
PUT /api/admin/categories/:id/home  # Настроить показ на главной
```

#### Legal Documents
```
GET    /api/legal                   # Активные документы для пользователя
GET    /api/legal/:type             # Документ по типу
GET    /api/admin/legal             # Список всех (админ)
POST   /api/admin/legal             # Создать
PUT    /api/admin/legal/:id         # Обновить (создает новую версию)
GET    /api/admin/legal/:id/versions # История версий
```

#### User Terms Acceptance
```
POST /api/user/accept-terms         # Принять юр. документы
GET  /api/user/terms-status         # Статус принятия
```

#### Wishlist Sharing
```
POST   /api/favorites/share          # Создать публичную ссылку
GET    /api/favorites/share          # Мои публичные ссылки
DELETE /api/favorites/share/:id      # Удалить
GET    /api/shared-wishlist/:shareId # Публичный wishlist
```

#### Cart (обновлено)
```
POST /api/cart/promocode            # Применить промокод
  # Response включает информацию о том, к каким товарам применен
  # и какие товары не позволяют применить промокод
```

---

## 🎯 ВАЛИДАЦИЯ ФОРМ - ОБНОВЛЕНО

### Промокод (админка) ❗
```typescript
{
  code: z.string()
    .min(4, 'Минимум 4 символа')
    .max(20, 'Максимум 20 символов')
    .regex(/^[A-Z0-9]+$/, 'Только заглавные буквы и цифры')
    .refine(async (code) => {
      // Проверка уникальности в БД
      const exists = await checkPromoCodeExists(code);
      return !exists;
    }, 'Промокод с таким кодом уже существует'),
  
  // Остальные поля без изменений
}
```

---

## 📊 АВТОМАТИЧЕСКИЕ ЗАДАЧИ - ОБНОВЛЕНО

### Ежедневно (00:00)
- Удаление просроченных корзин
- Удаление просроченных избранных
- Удаление старых заказов
- Деактивация просроченных wishlist shares ❗

### Каждый час
- Удаление просроченных админ кодов
- Проверка просроченных промокодов
- Проверка просроченных акций ❗

---

## 🎯 ФИЧИ ДЛЯ ВТОРОЙ ВЕРСИИ - ОБНОВЛЕНО

Из v2.0 **перенесено в v1.0:**
- ✅ Wishlist публичный (реализовано)

Остается в v2.0:
- Отзывы и рейтинги товаров
- Программа лояльности
- Сравнение товаров
- Чат с поддержкой
- Персональные рекомендации AI
- Предзаказ товаров
- Подписка на товары (recurring)
- Таймер обратного отсчета для акций
- Расширенная аналитика
- Многоязычность
- PWA
- Email-маркетинг
- Интеграция с CRM

---

## ✅ ИТОГОВЫЕ ИЗМЕНЕНИЯ В v1.1

### ✨ Добавлено:
1. ✅ Страница "Доставка и оплата" (одна страница)
2. ✅ Модальное меню (вместо обычного)
3. ✅ Акции на главной (горизонтальный скролл, плитки 1:1)
4. ✅ Метки категорий на главной (выбираются в админке)
5. ✅ Админ панель с табами (Контент/Статистика/Пользователи)
6. ✅ Промокод вводится вручную (не генерируется)
7. ✅ Баннеры 1:1 (квадратные, горизонтальный скролл)
8. ✅ Юридические документы (редактируемые админом)
9. ✅ Загрузочный экран с принятием юр. документов
10. ✅ Акционная цена (приоритет над скидкой, блокирует промокод)
11. ✅ Telegram fullscreen режим
12. ✅ Публичный wishlist с QR кодом

### 🗑️ Удалено:
1. ❌ SEO вкладка в форме товара (не нужна для Web App)

### 🔄 Изменено:
1. 🔄 Структура админки (табы вместо страниц)
2. 🔄 Логика применения промокодов (не работает с акционными товарами)
3. 🔄 Главная страница (акции + метки категорий + товары)
4. 🔄 База данных (новые таблицы и поля)

---

## 🚀 ЭТАПЫ РАЗРАБОТКИ - ОБНОВЛЕНО

### Phase 1: Foundation (Дни 1-2)
Без изменений

### Phase 2: Backend Core (Дни 3-5)
Добавлено:
- Promotions module
- Legal documents module
- Wishlist sharing logic
- Updated cart logic (с учетом акций)

### Phase 3: Frontend Core (Дни 6-10)
Добавлено:
- Splash screen + legal acceptance
- Modal menu
- Promotions carousel на главной
- Category badges на главной
- Delivery-payment page
- Legal documents page
- Wishlist sharing UI + QR code

### Phase 4: Admin Panel (Дни 11-13)
Изменено:
- Tabs вместо отдельных страниц
- Promotions management
- Legal documents editor
- Categories home page config
- Manual promocode input

### Phase 5-8: Без изменений

---

## 📝 ПРИМЕРЫ ДАННЫХ - ОБНОВЛЕНО

### Promotion
```json
{
  "id": "uuid",
  "title": "Скидка 30% на уход за кожей",
  "description": "Действует до конца месяца",
  "image": "/uploads/promotions/promo-1.jpg",
  "link": "/catalog/skincare",
  "order": 1,
  "isActive": true,
  "validFrom": "2024-11-01T00:00:00Z",
  "validTo": "2024-11-30T23:59:59Z"
}
```

### Product (с акцией)
```json
{
  "id": "uuid",
  "name": "Увлажняющий крем",
  "price": 1299.00,
  "promotionPrice": 899.00,
  "hasPromotion": true,
  "allowPromocode": false
}
```

### Wishlist Share
```json
{
  "id": "uuid",
  "userId": "uuid",
  "shareId": "abc123xyz",
  "title": "Мой список желаний",
  "qrCodeUrl": "/uploads/qr/abc123xyz.png",
  "isActive": true,
  "viewCount": 15,
  "createdAt": "2024-11-11T10:00:00Z"
}
```

---

## 🎉 ГОТОВО!

Техническое задание обновлено с учетом всех твоих замечаний!

**Ключевые улучшения:**
- 🚀 Загрузочный экран с юр. документами
- 🎨 Современная главная (акции + метки)
- 📱 Модальное меню
- 💰 Умная логика цен (акции vs скидки vs промокоды)
- 🔗 Публичный wishlist с QR
- ⚙️ Удобная админка с табами

**Следующие шаги:**
1. Используй это ТЗ для разработки с Claude Code
2. Все детали продуманы и готовы к реализации
3. Удачи с проектом! 🚀
