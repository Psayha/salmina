# 📋 CHANGELOG v1.0 → v1.1

## Дата: 2024-11-11

---

## ✨ ДОБАВЛЕНО

### 1. Загрузочный экран
- Splash screen с логотипом
- Получение Telegram initData
- Модальное окно принятия юридических документов
- Проверка `hasAcceptedTerms` перед доступом к приложению
- Telegram fullscreen mode (`viewport.expand()`)

### 2. Главная страница - Акции
- Новая секция "Акции" с горизонтальным скроллом
- Квадратные карточки 1:1 (вместо 16:9)
- Плитки с небольшим gap
- Админ управление в разделе "Акции"
- Таблица БД: `promotions`

### 3. Главная страница - Метки категорий
- Горизонтальный скролл меток категорий
- Квадратные изображения 1:1 (~80-100px)
- Админ выбирает какие показывать (`showOnHome`)
- Сортировка по `homeOrder`
- Поля в БД: `showOnHome`, `homeOrder`, `image`

### 4. Модальное меню
- Гамбургер меню вместо обычного
- Slide animation
- Backdrop затемнение
- Haptic feedback
- Пункты:
  - Главная
  - Каталог
  - Избранное
  - Заказы
  - Профиль
  - Доставка и оплата (новое)
  - Юр. документы (новое)
  - О нас

### 5. Страница "Доставка и оплата"
- Одна страница с двумя секциями
- Редактируемый контент через админку
- Rich text editor
- Тип документа: `DELIVERY_PAYMENT`

### 6. Юридические документы
- Модуль в админке
- Редактирование через Rich Text Editor
- Версионирование документов
- Типы: TERMS, PRIVACY, OFFER
- Таблица БД: `legal_documents`
- Страница для пользователей: `/legal`

### 7. Акционные цены товаров
- Новое поле: `promotionPrice`
- Новое поле: `hasPromotion`
- Приоритет: акция > скидка
- Логика: если акция - промокод НЕ работает
- Поля в cart_items: `appliedPrice`, `allowPromocode`

### 8. Умная логика промокодов
- Промокод применяется только к товарам без акции
- Валидация: есть ли товары с `allowPromocode = true`
- UI показывает какие товары блокируют промокод
- Расчет скидки только на разрешенные товары

### 9. Публичный Wishlist
- Кнопка "Поделиться" в избранном
- Генерация уникальной ссылки
- QR код для sharing
- Публичная страница `/shared-wishlist/[shareId]`
- Счетчик просмотров
- Таблица БД: `wishlist_shares`
- Поля: `shareId`, `title`, `qrCodeUrl`, `viewCount`

### 10. Админ панель - Табы
- Структура с табами:
  - Таб "Контент" (товары, категории, акции, заказы, промокоды, страницы, юр.документы)
  - Таб "Статистика" (метрики, графики)
  - Таб "Пользователи" (список, управление)
- Вместо отдельных страниц
- Более удобная навигация

### 11. Промокоды - Ручной ввод
- Админ вручную вводит код промокода
- НЕ случайная генерация
- Валидация уникальности
- Проверка формата (A-Z, 0-9)

### 12. Модуль "Акции" в админке
- CRUD операции
- Загрузка квадратного изображения (1:1)
- Выбор ссылки (категория/товар)
- Drag & drop для порядка
- Период показа

---

## 🔄 ИЗМЕНЕНО

### 1. Структура БД
Добавлены таблицы:
- `promotions`
- `wishlist_shares`
- `legal_documents`

Обновлены таблицы:
- `products`: +`promotionPrice`, +`hasPromotion`
- `categories`: +`image`, +`showOnHome`, +`homeOrder`
- `cart_items`: +`appliedPrice`, +`allowPromocode`
- `users`: +`hasAcceptedTerms`, +`termsAcceptedAt`
- `orders`: обновлена логика с промокодами

### 2. Главная страница
Порядок элементов:
1. Header
2. Акции (горизонтальный скролл)
3. Метки категорий (горизонтальный скролл)
4. Товары (grid)

### 3. Карточка товара
Логика отображения цены:
```
Приоритет 1: promotionPrice (если hasPromotion)
Приоритет 2: discountPrice (если isDiscount)
Приоритет 3: price (базовая)
```

### 4. Корзина
- Расчет с учетом акций
- Отображение какие товары блокируют промокод
- Валидация применения промокода
- UI с пояснениями

### 5. Админ панель
- Табы вместо страниц
- Раздел "Акции"
- Раздел "Юр. документы"
- Настройка категорий для главной
- Промокоды без генерации

### 6. API Endpoints
Добавлено:
- `/api/promotions`
- `/api/legal`
- `/api/favorites/share`
- `/api/shared-wishlist/:shareId`
- `/api/user/accept-terms`
- `/api/categories/home`

Обновлено:
- `/api/cart/promocode` (логика применения)

### 7. Telegram Mini App
- Fullscreen mode при запуске
- Splash screen
- Legal acceptance flow

---

## 🗑️ УДАЛЕНО

### 1. SEO вкладка
- Удалена из формы создания товара
- Поля `metaTitle`, `metaDescription` не нужны (Web App)
- Slug остается (для URL)

### 2. Генерация промокодов
- Удалена кнопка "Сгенерировать код"
- Админ вводит код вручную

---

## 🐛 ИСПРАВЛЕНО

### 1. Логика промокодов
- Теперь корректно обрабатываются акционные товары
- Промокод не применяется к товарам с `hasPromotion = true`
- Понятные сообщения об ошибках

### 2. Ценообразование
- Четкий приоритет: акция > скидка > базовая цена
- Snapshot цен в заказе
- Корректный расчет в корзине

---

## 📊 СТАТИСТИКА ИЗМЕНЕНИЙ

- **Новых таблиц БД:** 3
- **Обновленных таблиц:** 5
- **Новых API endpoints:** 8
- **Новых страниц:** 3
- **Новых компонентов:** ~10
- **Удаленных фич:** 2

---

## ⚠️ BREAKING CHANGES

### База данных
```sql
-- Требуется миграция БД:
ALTER TABLE products ADD COLUMN promotionPrice DECIMAL(10,2);
ALTER TABLE products ADD COLUMN hasPromotion BOOLEAN DEFAULT false;
ALTER TABLE categories ADD COLUMN image VARCHAR(255);
ALTER TABLE categories ADD COLUMN showOnHome BOOLEAN DEFAULT false;
ALTER TABLE categories ADD COLUMN homeOrder INTEGER;
ALTER TABLE cart_items ADD COLUMN appliedPrice DECIMAL(10,2);
ALTER TABLE cart_items ADD COLUMN allowPromocode BOOLEAN DEFAULT true;
ALTER TABLE users ADD COLUMN hasAcceptedTerms BOOLEAN DEFAULT false;
ALTER TABLE users ADD COLUMN termsAcceptedAt TIMESTAMP;

CREATE TABLE promotions (...);
CREATE TABLE wishlist_shares (...);
CREATE TABLE legal_documents (...);
```

### API
- Эндпоинт `/api/cart/promocode` изменил формат ответа
- Добавлены новые обязательные эндпоинты
- Обновлена логика применения скидок

### Frontend
- Обязательна обработка splash screen
- Обязательно модальное окно юр. документов
- Изменена структура главной страницы
- Изменена структура админ панели

---

## 🚀 КАК ОБНОВИТЬСЯ

### Для новых проектов:
Используйте v1.1 - всё включено

### Для существующих проектов:
1. Сделайте backup БД
2. Примените миграции
3. Обновите frontend компоненты
4. Обновите API endpoints
5. Добавьте новые модули админки
6. Тестируйте все флоу

---

## 📝 ЗАМЕТКИ

- Все изменения обратно-совместимы с данными v1.0
- Акции и промокоды работают независимо
- Юр. документы опциональны (можно отключить проверку)
- Wishlist sharing опционален
- Fullscreen можно отключить при необходимости

---

## 🎯 TODO для следующей версии (v2.0)

Из оригинального списка осталось:
- Отзывы и рейтинги
- Программа лояльности
- Сравнение товаров
- Чат с поддержкой
- AI рекомендации
- Предзаказ
- Recurring подписка
- Таймер обратного отсчета
- Расширенная аналитика
- Многоязычность
- PWA
- Email-маркетинг
- CRM интеграция

---

**Версия:** 1.1  
**Дата релиза:** 2024-11-11  
**Статус:** Готово к разработке ✅
